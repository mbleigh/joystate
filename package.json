{
  "name": "joystate",
  "version": "0.0.1",
  "description": "",
  "type": "module",
  "main": "./lib/cjs/index.js",
  "module": "./lib/mjs/index.js",
  "scripts": {
    "prepare": "npm run build",
    "clean": "rm -rf lib",
    "test": "ts-mocha test/**/*.spec.ts",
    "tsc": "tsc -p tsconfig-esm.json && tsc -p tsconfig.json",
    "build": "npm run tsc",
    "watch": "tsc -p tsconfig-esm.json --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/expect": "^24.3.0",
    "@types/mocha": "^7.0.2",
    "expect": "^26.0.1",
    "mocha": "^8.0.1",
    "prettier": "^2.0.5",
    "ts-mocha": "^7.0.0",
    "typescript": "^3.9.5"
  },
  "dependencies": {
    "@types/node": "^14.0.14"
  },
  "engines": {
    "node": ">= 10"
  }
}
